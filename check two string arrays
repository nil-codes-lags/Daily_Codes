String a="",b="";
        int l1 = word1.length;
        int l2 = word2.length;
        for(int i=0;i<Math.max(l1,l2);i++){
            if(i<l1){
            a+=word1[i];
            }
            if(i<l2){b+=word2[i];}
        }
        if(a.equals(b)){return true;}
        return false;
